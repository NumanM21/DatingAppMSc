import { RoleOfUserDirective } from './role-of-user.directive';

describe('RoleOfUserDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleOfUserDirective();
    expect(directive).toBeTruthy();
  });
});
